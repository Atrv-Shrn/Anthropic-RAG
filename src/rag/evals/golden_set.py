"""Hand-curated golden Q/A set (~50-100 pairs, including negative cases).

Semi-auto generated from ingested nodes, then manually curated into JSON/CSV. Used as the
shared ground truth for both RAGAS and the native LlamaIndex evaluators so baselines are
comparable.
"""

from __future__ import annotations


def load_golden_set():
    """Return the curated golden Q/A pairs."""
    raise NotImplementedError("M7")